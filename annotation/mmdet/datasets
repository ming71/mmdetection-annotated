################################
            transforms.py
################################

class ImageTransform(object)
    对图像进行基本的缩放/翻转/归一化/填充/维度变换（c,h,w）
    需要注意它实现了__call__方法，因此实例化的的对象是可调用的，执行__call__函数进行图像的处理和变换
